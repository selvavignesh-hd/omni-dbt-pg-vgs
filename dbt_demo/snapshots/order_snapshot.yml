snapshots:
- name: orders_snapshot
  relation: source('filing_wh', 'orders')
  config:
    schema: snapshots
    database: analytics
    unique_key: id
    strategy: timestamp
    updated_at: updated_at
